import ExamResultsPage from '@components/templates/ExamResults';
import React from 'react';

import { View } from 'react-native';



const ExamsResults = props => {
  return (

    <ExamResultsPage {...props} />
  );
};

export default ExamsResults;
